# Haskell Numerical Method Layer

This Haskell layer represents numerical method categories explicitly.

It helps prevent conceptual flattening:

- a time-step method is not a convergence diagnostic;
- discretization is not validation;
- a state constraint can hide numerical overshoot;
- numerical accuracy does not replace substantive model validity.
