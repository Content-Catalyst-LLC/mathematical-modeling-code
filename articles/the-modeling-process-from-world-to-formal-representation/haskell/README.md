# Haskell Modeling Process Layer

This Haskell layer represents modeling-process stages, model components, and review statuses as explicit types.

It is useful because it prevents conceptual flattening:

- a state variable is not a parameter;
- an assumption is not evidence;
- calibration is not validation;
- a model output is not a decision;
- a review-required item is not adequate for purpose.
