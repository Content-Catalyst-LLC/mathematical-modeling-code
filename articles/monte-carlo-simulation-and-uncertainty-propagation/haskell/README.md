# Haskell Monte Carlo Model Layer

This Haskell layer represents uncertainty-modeling categories explicitly.

It helps prevent conceptual flattening:

- an input distribution is not a sampling protocol;
- a random seed is not validation;
- a threshold probability is not the same as a decision;
- Monte Carlo simulation propagates assumptions rather than proving them.
