# Haskell Scientific Computing Workflow Layer

This Haskell layer represents workflow stages explicitly.

It helps prevent conceptual flattening:

- a data pipeline is not a validation diagnostic;
- a run manifest is not the same as a model output;
- a configuration file is not code;
- governance records are part of responsible modeling infrastructure.
