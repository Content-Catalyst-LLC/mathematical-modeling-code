# Haskell Model Selection Layer

This Haskell layer represents selection categories explicitly.

It helps prevent conceptual flattening:

- a candidate set is not a validation metric;
- a complexity penalty is not a decision;
- a selected model is not a universal truth;
- model selection governance is part of responsible modeling infrastructure.
