# Haskell Network Model Layer

This layer represents node definitions, edge definitions, edge weights, direction rules, centrality diagnostics, process rules, and validation diagnostics as distinct typed model records.
