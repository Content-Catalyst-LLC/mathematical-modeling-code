# Haskell Assumption and Model Design Layer

This Haskell layer represents assumption types, model-design objects, review status, and revision questions as explicit types.

It helps prevent conceptual flattening:

- a simplification is not a validation claim;
- a parameter is not an output;
- a boundary assumption is different from a functional-form assumption;
- a model output is not automatically a decision.
