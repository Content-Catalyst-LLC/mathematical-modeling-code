# Haskell Dynamic Model Layer

This Haskell layer represents dynamic model categories explicitly.

It helps prevent conceptual flattening:

- a state variable is not a rate equation;
- an initial condition is not a parameter;
- a boundary condition is part of model logic;
- a numerical setting can change model behavior.
